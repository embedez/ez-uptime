// This will be used later i hope idk but we going to import cloudflare and return it so we can do
// import stores from 'store' and then stores[type] to quick change that shiiiiii

import Cloudflare from "@/app/store/cloudflare";

const stores = {
    Cloudflare
};

export default stores
export {
    Cloudflare
}